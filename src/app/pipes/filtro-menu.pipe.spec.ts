import { FiltroMenuPipe } from './filtro-menu.pipe';

describe('FiltroMenuPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroMenuPipe();
    expect(pipe).toBeTruthy();
  });
});
