export enum Universities {
    ilumno = 0,
    areandina = 1,
    politecnico = 2
}

export enum AlertType {
    Success,
    Error,
    Info,
    Warning
}

export enum AuthProvider {
    NA = 0,
    dev = 1,
    IAM = 2
}

export enum ServiceTypeToInvoke {
    NA = 0,
    Houston = 1,
    Banner = 2
}

export enum RolPE {
    Administrador = 1,
    Estudiante = 2
}